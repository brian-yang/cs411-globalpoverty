import Layout from '../components/Layout';

export default function Example() {
    return (
        <Layout>
            <p>This could be the page to add or delete data</p>
        </Layout>
    );
}